# trading_backtesting

## Frontend

A React + Vite UI was added under `frontend/` for configuring strategies, running backtests, and reviewing results.

```bash
cd frontend
npm install
npm run dev
```
